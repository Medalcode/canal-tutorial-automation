[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python api_server.py"
restartPolicyMaxRetries = 5

[env]
PORT = 8000
PYTHONUNBUFFERED = 1

[volumes]
"/app/output" = "video_storage"
"/app/scripts" = "scripts_storage"
"/app/.jobs" = "jobs_storage"
